{
  "name": "petes-marine-service",
  "type": "module",
  "version": "1.0.0",
  "description": "Pete's Marine Service — boat repair website",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "preview:cf": "astro build && wrangler dev",
    "deploy": "astro build && wrangler pages deploy dist --project-name=petes-marine-service --branch=main --commit-dirty=true",
    "deploy:worker": "astro build && wrangler deploy",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.0",
    "astro": "^5.17.1"
  },
  "devDependencies": {
    "wrangler": "^4.107.0"
  }
}
